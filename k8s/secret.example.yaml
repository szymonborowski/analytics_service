# =============================================================================
# TEMPLATE ONLY - Do NOT commit real values to the repository!
#
# Usage:
#   1. Copy this file:  cp secret.example.yaml secret.yaml
#   2. Fill in real values in secret.yaml
#   3. Apply:  kubectl apply -f secret.yaml
#
# For production, use Sealed Secrets or External Secrets Operator instead.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: analytics-secret
  namespace: portfolio
  labels:
    app.kubernetes.io/name: analytics
    app.kubernetes.io/part-of: portfolio
type: Opaque
stringData:
  APP_KEY: "base64:CHANGE_ME_GENERATE_WITH_PHP_ARTISAN_KEY_GENERATE"
  DB_DATABASE: "analytics"
  DB_USERNAME: "analytics"
  DB_PASSWORD: "CHANGE_ME"
  RABBITMQ_USER: "guest"
  RABBITMQ_PASSWORD: "guest"
  INTERNAL_API_KEY: "CHANGE_ME_GENERATE_SECURE_KEY"
