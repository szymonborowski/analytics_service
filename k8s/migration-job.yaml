apiVersion: batch/v1
kind: Job
metadata:
  name: analytics-migration
  namespace: portfolio
  labels:
    app.kubernetes.io/name: analytics-migration
    app.kubernetes.io/part-of: portfolio
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: analytics-migration
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: ghcr.io/szymonborowski/microservices-analytics:v0.0.1
          command: ["php", "artisan", "migrate", "--force"]
          envFrom:
            - configMapRef:
                name: analytics-config
            - secretRef:
                name: analytics-secret
